{
  "name": "tick",
  "version": "0.1.1",
  "description": "Todoist CLI — tasks, projects, labels, sections from your shell",
  "module": "src/cli.ts",
  "type": "module",
  "license": "MIT",
  "author": "Serhii Troinin",
  "bin": {
    "tick": "src/cli.ts"
  },
  "scripts": {
    "dev": "bun run src/cli.ts",
    "typecheck": "bunx tsc --noEmit",
    "build": "bun build src/cli.ts --compile --outfile dist/tick"
  },
  "dependencies": {
    "chalk": "^5.6.2",
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
